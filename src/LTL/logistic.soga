array[30] X;

currX = 0.8;

for i in range(30) {

    X[i] = currX;
    xSquare = currX * currX;
    noise = gauss(0., 0.0324);
    xNoise = currX * noise;
    newX = 1.32 * currX - 0.032 * xSquare + xNoise;

    currX = newX;


} end for;